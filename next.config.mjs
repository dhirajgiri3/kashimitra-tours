/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res-console.cloudinary.com', 'images.unsplash.com', 'cdn.pixabay.com'],
    },
};

export default nextConfig;
